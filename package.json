{
  "name": "add-linear-label",
  "version": "1.0.0",
  "description": "GitHub Action to add labels to Linear issues",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && ncc build"
  },
  "keywords": [
    "actions",
    "linear",
    "labels"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@linear/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.0.0"
  }
}
